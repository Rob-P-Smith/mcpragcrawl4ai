# Client Configuration - connects to remote server
IS_SERVER=false

# Remote Server Configuration
REMOTE_API_URL=http://SERVERIP:SERVERPORT
REMOTE_API_KEY=test-key-12345-for-development-only

# Client settings (not used in client mode, but kept for compatibility)
SERVER_HOST=localhost
SERVER_PORT=3000

# Logging
LOG_LEVEL=INFO